import { supabase } from './supabase.js'; import './style.css';
import {cachedRead} from './public-cache.js'; import {contentFrom} from './site-content.js';
const app=document.querySelector('#app'),q=new URLSearchParams(location.search),slug=q.get('slug')||'',name=q.get('name')||'Collection';
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const safe=u=>{try{return new URL(u||'',location.origin).href}catch{return '#'}};const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'EGP',maximumFractionDigits:0}).format(Number(n)||0);
async function load(){let products=[],settings={logo_url:'elvira-logo-black.png'};if(supabase){const [p,s]=await Promise.all([supabase.from('products').select('*,categories(name,slug,gender),product_images(*)').order('created_at',{ascending:false}),supabase.from('site_settings').select('*').eq('id',1).single()]);products=p.data||[];settings=s.data||settings}const c=contentFrom(settings),cat=products.filter(p=>(p.categories?.slug||'')===slug||(p.categories?.name||'').toLowerCase()===name.toLowerCase());render(cat,settings,c)}
function render(products,s,c){app.innerHTML=`<header class="nav"><a class="brand" href="./"><img src="elvira-logo-black.png" alt="Elvira"></a><nav><a href="./">${esc(c.nav_home)}</a><a href="collection.html">${esc(c.nav_collection)}</a><a href="policy.html">${esc(c.nav_policy)}</a></nav></header><main><section class="page-hero"><small>${esc(c.category_eyebrow)}</small><h1>${esc(name)}</h1><p>${esc(c.category_description)}</p></section><section class="collection"><div class="section-head"><div><small>${products.length} ${esc(c.category_count_suffix)}</small><h2>${esc(name)} collection</h2></div><a class="text-link" href="collection.html">${esc(c.category_view_all)}</a></div><div class="grid">${products.map(p=>{const img=p.image_url||p.product_images?.slice().sort((a,b)=>(a.sort_order||0)-(b.sort_order||0))[0]?.url||'elvira-logo-black.png';return `<article class="product"><a class="product-img square-product" href="product.html?id=${encodeURIComponent(p.id)}"><img src="${esc(safe(img))}" alt="${esc(p.name)}"></a><div class="product-meta"><span class="eyebrow">${esc(p.gender==='male'?'MALE FRAGRANCE':'FEMALE FRAGRANCE')}</span><h3>${esc(p.name)}</h3><p>${esc(p.notes||p.description||'')}</p><div class="price-row"><strong>${money(p.price)}</strong><span>${p.stock>0?`${p.stock} ${esc(c.product_available_suffix)}`:esc(c.product_sold_out)}</span></div></div></article>`}).join('')||`<div class="empty">${esc(c.category_empty)}</div>`}</div></section></main><footer><img src="elvira-logo-black.png" alt="Elvira"><span>© ${new Date().getFullYear()} ${esc(c.footer_copyright)}</span></footer>`}
load();
